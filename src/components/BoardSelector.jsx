import React from 'react';
import { BoardSelector as RefactoredBoardSelector } from './board-selector/index.jsx';

export const BoardSelector = (props) => {
  return <RefactoredBoardSelector {...props} />;
};
